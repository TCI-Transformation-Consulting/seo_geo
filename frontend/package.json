{
  "name": "neuro-web-frontend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd /app && yarn dev -p 3000",
    "dev": "cd /app && yarn dev -p 3000",
    "build": "cd /app && yarn build",
    "lint": "cd /app && yarn lint"
  }
}
